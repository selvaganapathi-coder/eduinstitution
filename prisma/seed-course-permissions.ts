import "dotenv/config";
import { PrismaNeon } from "@prisma/adapter-neon";
import { PrismaClient } from "../src/generated/prisma-node/client.ts";
const permissions=[['course:view','View courses and subjects'],['course:create','Create courses and subjects'],['course:update','Edit courses and subjects'],['course:archive','Archive courses and subjects']] as const;
async function main(){if(process.env.NODE_ENV==='production')throw new Error('The development seed must not run in production.');const connectionString=process.env.DIRECT_DATABASE_URL??process.env.DATABASE_URL;if(!connectionString)throw new Error('DIRECT_DATABASE_URL or DATABASE_URL is required to run the development seed.');const prisma=new PrismaClient({adapter:new PrismaNeon({connectionString})});try{const roles=await prisma.role.findMany({where:{scope:'SYSTEM'},select:{id:true}});for(const [code,description] of permissions){const permission=await prisma.permission.upsert({where:{code},update:{description},create:{code,description}});for(const role of roles)await prisma.rolePermission.upsert({where:{roleId_permissionId:{roleId:role.id,permissionId:permission.id}},update:{},create:{roleId:role.id,permissionId:permission.id}});}console.log('Course permissions synced.');}finally{await prisma.$disconnect();}}main().catch(error=>{console.error(error);process.exitCode=1;});
